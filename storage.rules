rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to any file
    // In production, you should add proper authentication rules
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}